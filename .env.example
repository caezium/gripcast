# Copy to .env (or .env.production) and rebuild.

# GripCast lap-data contribution Worker endpoint (see worker/README.md).
# Leave unset to disable uploads — the Contribute UI still parses files locally.
VITE_CONTRIBUTE_URL=
